# Postgres connection string.
# Locally:  postgresql://postgres:postgres@localhost:5432/grocery?schema=public
# Railway:  copy the Postgres service's DATABASE_URL, or reference it as
#           ${{ Postgres.DATABASE_URL }} in the web service variables.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/grocery?schema=public"
